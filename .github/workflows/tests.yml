name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Checkout AgentTrace
        uses: actions/checkout@v4
        with:
          path: AgentTrace

      - name: Checkout agentsec-bench
        uses: actions/checkout@v4
        with:
          repository: samyshyaka/agentsec-bench
          path: agentsec-bench

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        working-directory: AgentTrace
        run: uv sync

      - name: Run tests
        working-directory: AgentTrace
        run: uv run pytest -v